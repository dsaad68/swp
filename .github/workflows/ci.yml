name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    name: Build & Test · ${{ matrix.name }}
    runs-on: ${{ matrix.runs-on }}
    container: ${{ matrix.container }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS
            runs-on: macos-14
            container: ''
          - name: Linux
            runs-on: ubuntu-latest
            container: swift:6.2
    steps:
      - uses: actions/checkout@v4

      - name: Select latest-stable Xcode (macOS)
        if: runner.os == 'macOS'
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Swift version
        run: swift --version

      - name: Cache SwiftPM build
        uses: actions/cache@v4
        with:
          path: .build
          key: ${{ matrix.name }}-spm-${{ hashFiles('Package.swift') }}
          restore-keys: |
            ${{ matrix.name }}-spm-

      - name: Build
        run: swift build

      - name: Run tests
        run: swift test

      # End-to-end: drive the built binary the way a shell does. This is the
      # only place the redirected paths and the exit codes are exercised as a
      # real process — and the only place a signal is actually sent, to a sleep
      # the script starts and owns.
      - name: Integration checks (CLI)
        run: ./Tests/Integration/cli.sh .build/debug/swp

  lint:
    name: Lint & Format
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Install SwiftLint & SwiftFormat
        run: brew install swiftlint swiftformat

      - name: SwiftFormat (check)
        run: swiftformat --lint .

      - name: SwiftLint (strict)
        run: swiftlint lint --strict
